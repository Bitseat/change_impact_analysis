connect user customer search results table like view single column first last preferred name view customer name single column sort accordingly acceptance criteria customer results search table create column named name display customer name following last format name first name preferred name column sort filter capabilities currently columns table remove affiliation last name first name preferred name columns
